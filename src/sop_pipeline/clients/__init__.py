"""HTTP clients for the external APIs the pipeline reads from.

Import the concrete modules directly; this package intentionally re-exports
nothing, to keep import side effects out of the package initialiser.
"""
