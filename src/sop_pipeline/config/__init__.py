"""Environment-backed configuration for the pipeline.

Import ``sop_pipeline.config.settings`` directly. Instantiating the settings
reads and validates the ``.env`` file, so that import is kept explicit rather
than triggered by importing the package.
"""
